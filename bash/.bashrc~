# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions
alias clear_login='ssh -Y ttl4@ssh.clear.rice.edu'
alias pi_login='ssh pi@10.119.77.231'
alias wcd='cd /run/media/tamtale/04846F03846EF698'
alias db='export TDSVER=8.0; tsql -H classdb.ad.rice.edu -U adrice\\ttl4 -p 1433
;'
alias ricevpn='sudo vpnc RiceVPN'
alias agenda='gcalcli agenda --calendar Tam --calendar plan --calendar tests/quizzes'
alias dlog='docker logs a716f2bb7d8d'
alias vim='vim --servername VIM'

export PATH="$PATH:/opt/mssql-tools/bin"
